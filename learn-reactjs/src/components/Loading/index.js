import PropTypes from 'propTypes-types'
import './styles.scss'

Loading.propTypes = {

}

function Loading() {
    return (
        <div>
            Loading
        </div>
    )
}

export default Loading